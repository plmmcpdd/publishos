name: Build & Release

on:
  workflow_dispatch:
    inputs:
      publish_release:
        description: 'Publish a GitHub Release with the Windows installer'
        required: true
        type: boolean
        default: true
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: cd client && npm install
        shell: bash
      - name: Build Windows
        run: cd client && npm run build:win
        shell: bash
        env:
          VITE_API_URL: "http://104.238.181.32:3000/v1"
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: publishos-windows
          path: client/release/
          retention-days: 3

  release:
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.publish_release)
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || format('build-{0}', github.run_number) }}
          target_commitish: ${{ github.sha }}
          name: ${{ startsWith(github.ref, 'refs/tags/') && format('PublishOS {0}', github.ref_name) || format('PublishOS Build {0}', github.run_number) }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/publishos-windows/**/*.exe
